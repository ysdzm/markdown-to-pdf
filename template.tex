\documentclass{article}
\usepackage[top=25truemm, bottom=25truemm, left=20truemm, right=20truemm]{geometry}
\usepackage[utf8]{inputenc}
\usepackage[japanese]{babel}  % 日本語のサポートを有効にする

\def\tightlist{\itemsep1pt\parskip0pt\parsep0pt}  % ここで \tightlist を定義

% タイトル設定
\title{$title$}  % Markdownで定義されたタイトルを挿入

% セクション設定（デフォルトの \section ではなく、指定された \section を使う）
\renewcommand{\section}[1]{\vspace{10pt}\noindent \textbf{\large #1}}

% サブセクション設定（pandoc によるデフォルトで \subsection を使用しない）
\renewcommand{\subsection}[1]{\vspace{8pt}\noindent \textbf{#1}}

\begin{document}

\maketitle

$body$

\end{document}
